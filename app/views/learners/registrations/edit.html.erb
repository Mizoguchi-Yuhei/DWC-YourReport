<div class="container">
  <div class="row col-md-6 mx-auto mb-3">
    <h3>登録情報編集 <%= resource_name.to_s.humanize %>さん</h3>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "learners/shared/error_messages", resource: resource %>

      <div class="field mt-3">
        <%= f.label :name, "ニックネーム" %><br />
        <%= f.text_field :name, autofocus: true %>
      </div>

      <div class="field mt-3">
        <%= f.label :email, "メールアドレス" %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
      </div>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <div>現在確認待ち： <%= resource.unconfirmed_email %></div>
      <% end %>

      <div class="field mt-3">
        <%= f.label :password, "パスワード" %> <span>(パスワードを変更しない場合は空欄のまま)</span><br />
        <%= f.password_field :password, autocomplete: "new-password" %>
        <% if @minimum_password_length %>
          <br />
          <span><%= @minimum_password_length %>文字以上</span>
        <% end %>
      </div>

      <div class="field mt-3">
        <%= f.label :password_confirmation, "パスワード(確認用)" %><span>(パスワードを変更しない場合は空欄のまま)</span><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
      </div>

      <div class="field mt-3">
        <%= f.label :current_password, "現在のパスワード" %> <i>(確認のため現在のパスワードを入力してください)</i><br />
        <%= f.password_field :current_password, autocomplete: "current-password" %>
      </div>

      <div class="actions mt-3 mb-5">
        <%= f.submit "更新する", class: "btn btn-success" %>
      </div>
    <% end %>

    <h5>アカウントの削除</h5>
    <p>あなたのアカウントを削除しますか？<%= button_to "削除する", registration_path(resource_name), data: { confirm: "アカウントを削除するとデータはすべて失われます。削除本当に削除しますか?" }, method: :delete, class: "btn btn-danger" %></p>
    <%= link_to "戻る", :back %>
  </div>
</div>