<div class="container">
  <div class"row">
    <h3>次の定期テストの目標</h3>
    <div>
      <%= form_with model: [@learner, @goal] do |goal| %>
        <%= goal.text_area :body %>
        <%= goal.submit "登録" %>
      <% end %>
    </div>
  </div>
</div>