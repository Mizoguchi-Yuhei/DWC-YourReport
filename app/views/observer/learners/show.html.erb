<div class="container">
	<div class="row mt-3">
		<h4>
			<%= @follow.name %>
		</h4>
		<div>
			<ul>
				<% @tests.each do |test| %>
					<li>
						<%= link_to test.name, observer_tests_show_path(test) %>
					</li>
				<% end %>
			</ul>
		</div>
	</div>
</div>