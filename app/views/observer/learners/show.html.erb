<div class="container">
	<div class="row mt-3">
		<h4>
			<%= @follow.name %>
		</h4>
		<div>
			<% @tests.each do |test| %>
				<%= link_to test.name, observer_tests_show_path(test) %>
			<% end %>
		</div>
	</div>
</div>